# FieldOps Kunde

React Native CLI app for customers (`de.fieldops.customer`). No Expo.

```powershell
cd C:\Users\user\Desktop\FieldOps\mobileapp_client\customer
npm install
npm start
npm run android
```

Login: `james.b@example.com` / `FieldOps!2026`

API host is `src/config.ts` (`10.0.2.2` on Android emulator, `127.0.0.1` on iOS simulator).
